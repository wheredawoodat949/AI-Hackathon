"""SoccerTrack v2 game-state analysis pipeline (SAM 3.1 → minimap → HOTA eval).

All reusable logic lives here as importable modules. Notebooks and the future
frontend import from `src` and call functions — they hold no core logic.
"""
